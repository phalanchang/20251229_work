JWSについて、私の理解の確認をさせてください。
ユーザがログイン画面に来る＝未ログイン状態の状態から話は始まると理解しています。
で、ユーザID(メールアドレス)とパスワードを入力してログインのボタンを押下することで、サーバ側へ処理が遷移すると思っています。
このタイミングで、サーバ側はユーザIDとパスワードのチェック（DB問い合わせを実施し、正しいIDとパスワードのペアが入力されているかチェック。この時ハッシュも利用する）。
正しいIDとパスワード（のハッシュ値）のペアであれば、このタイミングでJWSトークンを発行すると思っています。
で、このトークンをサーバ側で持っておくと思っているのですが…あってますか？

トークンなのか、トークンのハッシュ値なのかは分かりませんが（どちらでもよいとは思いますが）、ユーザIDと、トークンと、有効期限をテーブルか、サーバの変数かに持っておいて、トークンを返します。
で、JWSトークンはユーザ側がブラウザで持っておいて、問い合わせのたびにブラウザが？リクエストに付与して送る…と認識しています。
そして、サーバ側ではブラウザが自動で付与したトークン（多分HTMLヘッダにあると思っている）を取り出し、認証チェックをする…の流れとおもっていますが、あってますか？

